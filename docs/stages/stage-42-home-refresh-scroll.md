# Stage 42 — 首页刷新回到顶部

## 目标

- 首页刷新后默认回到页面顶部，不再停留在事件列表。
- 保留“查看今日情报”“浏览今日变化”“继续浏览情报”的页内滚动能力。

## 实现

- 三个入口改为受控页内滚动，滚动时不再把 `#events` 留在地址栏。
- 首页刷新或历史地址仍包含 `#events` 时，清理锚点并恢复到顶部。
- 关闭浏览器自动滚动恢复，避免刷新时复用旧的垂直位置。
